# Current UI task

No active Antigravity UI task is assigned.

When Codex delegates UI work, replace this section with a concrete task:

- Goal:
- Screens:
- Existing bindings/commands to use:
- Files Antigravity may edit:
- Visual constraints:
- Acceptance checks:

Antigravity must follow `docs/ui-contract.md` and must not edit forbidden logic paths.

