<UserControl x:Class="DACDT_2026.Views.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DACDT_2026">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DACDT_2026;component/Views/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <local:BoolToStatusBrushConverter x:Key="BoolToStatusBrushConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.05*"/>
            <ColumnDefinition Width="1.25*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="260"/>
                </Grid.ColumnDefinitions>

                <ItemsControl ItemsSource="{Binding Axes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource PanelAltBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="10"
                                    Margin="0,0,8,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="74"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource PanelTitleStyle}"/>
                                        <TextBlock Text="{Binding AxisStatus}" Style="{StaticResource PanelSubtitleStyle}" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                        <TextBlock Text="POSITION" Style="{StaticResource AxisMetricLabelStyle}"/>
                                        <TextBlock Text="{Binding CurrentPos}" Style="{StaticResource AxisMetricPrimaryValueStyle}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                        <TextBlock Text="SPEED" Style="{StaticResource AxisMetricLabelStyle}"/>
                                        <TextBlock Text="{Binding CurrentSpeed}" Style="{StaticResource AxisMetricValueStyle}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="3" Margin="8,0,0,0">
                                        <TextBlock Text="MD.44" Style="{StaticResource AxisMetricLabelStyle}"/>
                                        <TextBlock Text="{Binding CurrentDataNo}" Style="{StaticResource AxisMetricValueStyle}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Grid.Column="1"
                        Background="{DynamicResource PanelAltBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="DXF Completion" Style="{StaticResource PanelTitleStyle}"/>
                        <ProgressBar Grid.Row="1"
                                     Height="12"
                                     Maximum="100"
                                     Value="{Binding ProgressPercent}"
                                     Margin="0,8,0,0"/>
                        <Grid Grid.Row="2" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding ActiveProgramText}"
                                       Foreground="{DynamicResource MutedBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding ProgressText}"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource AccentBrush}"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding ProgramMonitorTitle}" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="{Binding ProgramMonitorSubtitle}" Style="{StaticResource PanelSubtitleStyle}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ActiveProgramText}" Foreground="{DynamicResource MutedBrush}" Margin="0,0,8,0"/>
                        <Ellipse Width="12" Height="12" Fill="{Binding IsConnected, Converter={StaticResource BoolToStatusBrushConverter}}"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding RunProgressText}" Foreground="{DynamicResource MutedBrush}" Visibility="{Binding RunProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <TextBlock Grid.Column="1" Text="{Binding ProgressText}" Foreground="{DynamicResource MutedBrush}"
                                   Visibility="{Binding ProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                    <ProgressBar Height="8"
                                 Maximum="100"
                                 Value="{Binding RunProgressPercent, Mode=OneWay}"
                                 Visibility="{Binding RunProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Margin="0,5,0,0"/>
                    <ProgressBar Height="5"
                                 Maximum="100"
                                 Value="{Binding ProgressPercent}"
                                 Visibility="{Binding ProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Margin="0,4,0,0"/>
                </StackPanel>

                <DataGrid x:Name="ProgramGrid"
                          Grid.Row="2"
                          ItemsSource="{Binding ProgramRows}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.ScrollChanged="ProgramGrid_ScrollChanged"
                          HorizontalScrollBarVisibility="Auto"
                          Margin="0,8,0,0"
                          Background="{DynamicResource PanelAltBrush}">
                    <DataGrid.RowStyle>
                        <Style TargetType="{x:Type DataGridRow}">
                            <Setter Property="MinHeight" Value="28"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter Property="Background" Value="#1E4E3A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Run" Binding="{Binding ActiveMarker}" Width="48"/>
                        <DataGridTextColumn Header="No." Binding="{Binding Index}" Width="58"/>
                        <DataGridTextColumn Header="DXF Point" Binding="{Binding MotionType}" Width="*"/>
                        <DataGridTextColumn Header="M" Binding="{Binding MCodeValue}" Width="48"/>
                        <DataGridTextColumn Header="Speed" Binding="{Binding Speed}" Width="72"/>
                        <DataGridTextColumn Header="End X;Y" Binding="{Binding EndCoordinate}" Width="135"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Camera Monitor" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="{Binding CameraStatus}" Style="{StaticResource PanelSubtitleStyle}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="{Binding IsCameraRunning, Converter={StaticResource BoolToStatusBrushConverter}}" Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding CameraRecordingText}" Foreground="{DynamicResource MutedBrush}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="0,10,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="88"/>
                        <ColumnDefinition Width="76"/>
                        <ColumnDefinition Width="76"/>
                        <ColumnDefinition Width="86"/>
                        <ColumnDefinition Width="96"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox ItemsSource="{Binding Cameras}"
                              DisplayMemberPath="DisplayName"
                              SelectedItem="{Binding SelectedCamera, Mode=TwoWay}"
                              Margin="0,0,6,0"/>
                    <Button Grid.Column="1" Content="REFRESH" Command="{Binding RefreshCamerasCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0"/>
                    <Button Grid.Column="2" Content="START" Command="{Binding StartCameraCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,6,0"/>
                    <Button Grid.Column="3" Content="STOP" Command="{Binding StopCameraCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0"/>
                    <Button Grid.Column="4" Content="REC" Command="{Binding StartCameraRecordingCommand}" Style="{StaticResource DangerButtonStyle}" Margin="0,0,6,0"/>
                    <Button Grid.Column="5" Content="STOP REC" Command="{Binding StopCameraRecordingCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
                </Grid>

                <StackPanel Grid.Row="2" Margin="0,0,0,8">
                    <TextBlock Text="Recording folder" Style="{StaticResource PanelSubtitleStyle}" Margin="0,0,0,4"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="88"/>
                            <ColumnDefinition Width="86"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding CameraRecordingFolderInput, UpdateSourceTrigger=PropertyChanged}"
                                 VerticalContentAlignment="Center"
                                 ToolTip="Recording folder"/>
                        <Button Grid.Column="1"
                                Content="BROWSE"
                                Command="{Binding BrowseCameraRecordingFolderCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="6,0,6,0"/>
                        <Button Grid.Column="2"
                                Content="SET PATH"
                                Command="{Binding SetCameraRecordingFolderCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"/>
                    </Grid>
                </StackPanel>

                <Border Grid.Row="3"
                        Background="#050A13"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        ClipToBounds="True">
                    <Grid>
                        <Image Source="{Binding CameraFrame}"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Camera preview"
                                   Foreground="{DynamicResource MutedBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CameraFrame}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <TextBlock Grid.Row="4"
                           Text="{Binding CameraRecordingPath}"
                           Foreground="{DynamicResource MutedBrush}"
                           TextTrimming="CharacterEllipsis"
                           Margin="0,8,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
