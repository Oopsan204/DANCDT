<UserControl x:Class="DACDT_2026.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DACDT_2026;component/Views/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="1.25*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel>
                    <TextBlock Text="Axis Monitor" Style="{StaticResource PanelTitleStyle}"/>
                    <TextBlock Text="Real-time coordinate and axis status monitoring" Style="{StaticResource PanelSubtitleStyle}"/>
                </StackPanel>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Axes}" Margin="0,8,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource PanelAltBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="10"
                                    Margin="0,0,8,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Border Background="{DynamicResource CardHeaderBrush}"
                                            CornerRadius="6,6,0,0"
                                            Padding="0,0,0,8"
                                            Margin="-10,-10,-10,10">
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="{DynamicResource CardHeaderTextBrush}"
                                                   FontSize="15"
                                                   FontWeight="Bold"
                                                   Margin="10,8,10,0"/>
                                    </Border>

                                    <StackPanel Grid.Row="1">
                                        <StackPanel Margin="0,0,0,9">
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="CURRENT POSITION (MM) "/>
                                                <Run Text="{Binding CurrentPosAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding CurrentPos}" Style="{StaticResource AxisMetricValueStyle}"/>
                                        </StackPanel>

                                        <StackPanel Margin="0,0,0,9">
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="CURRENT SPEED (MM/MIN) "/>
                                                <Run Text="{Binding CurrentSpeedAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding CurrentSpeed}" Style="{StaticResource AxisMetricValueStyle}"/>
                                        </StackPanel>

                                        <StackPanel Margin="0,0,0,9">
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="CURRENT M CODE "/>
                                                <Run Text="{Binding MCodeAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding MCode}" Style="{StaticResource AxisMetricValueStyle}"/>
                                        </StackPanel>

                                        <StackPanel Margin="0,0,0,9">
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="ERROR CODE "/>
                                                <Run Text="{Binding ErrorCodeAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding ErrorCode}" ToolTip="{Binding ErrorDescription}" Style="{StaticResource AxisMetricValueStyle}"/>
                                            <TextBlock Text="{Binding ErrorDescription}" TextWrapping="Wrap" Foreground="#f85149" FontSize="10" Margin="0,2,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <Trigger Property="Text" Value="">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                            <Trigger Property="Text" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Margin="0,0,0,9">
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="WARNING CODE "/>
                                                <Run Text="{Binding WarningCodeAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding WarningCode}" ToolTip="{Binding WarningDescription}" Style="{StaticResource AxisMetricValueStyle}"/>
                                            <TextBlock Text="{Binding WarningDescription}" TextWrapping="Wrap" Foreground="#ff9800" FontSize="10" Margin="0,2,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <Trigger Property="Text" Value="">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                            <Trigger Property="Text" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Margin="0,0,0,9">
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="AXIS STATUS "/>
                                                <Run Text="{Binding AxisStatusAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding AxisStatus}" Style="{StaticResource AxisMetricValueStyle}"/>
                                        </StackPanel>

                                        <StackPanel Margin="0,0,0,9">
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="MD.44 CURR DATA NO. "/>
                                                <Run Text="{Binding CurrentDataNoAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding CurrentDataNo}" Style="{StaticResource AxisMetricValueStyle}"/>
                                        </StackPanel>

                                        <StackPanel>
                                            <TextBlock Style="{StaticResource AxisMetricLabelStyle}">
                                                <Run Text="MD.46 LAST DATA NO. "/>
                                                <Run Text="{Binding LastDataNoAddr}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding LastDataNo}" Style="{StaticResource AxisMetricValueStyle}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <Border Grid.Row="2"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,0"
                                            Padding="0,7,0,0"
                                            Margin="0,8,0,0">
                                        <WrapPanel>
                                            <Ellipse Width="7" Height="7" Fill="{Binding LimitMinus, Converter={StaticResource BoolToStatusBrushConverter}}" Margin="0,0,4,0"/>
                                            <TextBlock Text="LIM-" Foreground="{DynamicResource MutedBrush}" FontSize="10" Margin="0,0,8,0"/>
                                            <Ellipse Width="7" Height="7" Fill="{Binding LimitPlus, Converter={StaticResource BoolToStatusBrushConverter}}" Margin="0,0,4,0"/>
                                            <TextBlock Text="LIM+" Foreground="{DynamicResource MutedBrush}" FontSize="10" Margin="0,0,8,0"/>
                                            <Ellipse Width="7" Height="7" Fill="{Binding HomeDog, Converter={StaticResource BoolToStatusBrushConverter}}" Margin="0,0,4,0"/>
                                            <TextBlock Text="HOME" Foreground="{DynamicResource MutedBrush}" FontSize="10" Margin="0,0,8,0"/>
                                            <Ellipse Width="7" Height="7" Fill="{Binding IsComplete, Converter={StaticResource BoolToStatusBrushConverter}}" Margin="0,0,4,0"/>
                                            <TextBlock Text="DONE" Foreground="{DynamicResource MutedBrush}" FontSize="10"/>
                                        </WrapPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding ProgramMonitorTitle}" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="{Binding ProgramMonitorSubtitle}" Style="{StaticResource PanelSubtitleStyle}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ActiveProgramText}" Foreground="{DynamicResource MutedBrush}" Margin="0,0,8,0"/>
                        <Ellipse Width="12" Height="12" Fill="{Binding IsConnected, Converter={StaticResource BoolToStatusBrushConverter}}"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding RunProgressText}" Foreground="{DynamicResource MutedBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding ProgressText}" Foreground="{DynamicResource MutedBrush}"
                                   Visibility="{Binding ProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                    <ProgressBar Height="8"
                                 Maximum="100"
                                 Value="{Binding RunProgressPercent, Mode=OneWay}"
                                 Margin="0,5,0,0"/>
                    <ProgressBar Height="5"
                                 Maximum="100"
                                 Value="{Binding ProgressPercent}"
                                 Visibility="{Binding ProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Margin="0,4,0,0"/>
                </StackPanel>

                <DataGrid x:Name="ProgramGrid"
                          Grid.Row="2"
                          ItemsSource="{Binding ProgramRows}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.ScrollChanged="ProgramGrid_ScrollChanged"
                          HorizontalScrollBarVisibility="Auto"
                          Margin="0,8,0,0"
                          Background="{DynamicResource PanelAltBrush}">
                    <DataGrid.RowStyle>
                        <Style TargetType="{x:Type DataGridRow}">
                            <Setter Property="MinHeight" Value="28"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter Property="Background" Value="#1E4E3A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Run" Binding="{Binding ActiveMarker}" Width="48"/>
                        <DataGridTextColumn Header="No." Binding="{Binding Index}" Width="58"/>
                        <DataGridTextColumn Header="G-code / Point" Binding="{Binding MotionType}" Width="*"/>
                        <DataGridTextColumn Header="M" Binding="{Binding MCodeValue}" Width="48"/>
                        <DataGridTextColumn Header="Speed" Binding="{Binding Speed}" Width="72"/>
                        <DataGridTextColumn Header="End X;Y" Binding="{Binding EndCoordinate}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource PanelBorderStyle}">
            <StackPanel>
                <TextBlock Text="System Connectivity" Style="{StaticResource PanelTitleStyle}"/>
                <TextBlock Text="Mitsubishi PLC Network Interface" Style="{StaticResource PanelSubtitleStyle}"/>
                <TextBlock Text="Logical station no." Foreground="{DynamicResource MutedBrush}" FontSize="11" Margin="0,8,0,3"/>
                <TextBox Text="{Binding LogicalStationInput, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="{Binding ConnectionButtonText}" Command="{Binding ConnectToggleCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,8,0,0"/>

                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="HOME ALL" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,4,0" Tag="homeall" PreviewMouseDown="HoldButton_PreviewMouseDown" PreviewMouseUp="HoldButton_PreviewMouseUp" MouseLeave="HoldButton_MouseLeave"/>
                    <Button Grid.Column="1" Content="HOME" Style="{StaticResource SecondaryButtonStyle}" Margin="2,0,2,0" Tag="home" PreviewMouseDown="HoldButton_PreviewMouseDown" PreviewMouseUp="HoldButton_PreviewMouseUp" MouseLeave="HoldButton_MouseLeave"/>
                    <Button Grid.Column="2" Content="RESET" Style="{StaticResource SecondaryButtonStyle}" Margin="4,0,0,0" Tag="reset" PreviewMouseDown="HoldButton_PreviewMouseDown" PreviewMouseUp="HoldButton_PreviewMouseUp" MouseLeave="HoldButton_MouseLeave"/>
                </Grid>
                <Button Content="START ACTION" IsEnabled="{Binding IsStartActionEnabled}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,6,0,0" Tag="start" PreviewMouseDown="HoldButton_PreviewMouseDown" PreviewMouseUp="HoldButton_PreviewMouseUp" MouseLeave="HoldButton_MouseLeave"/>
                <Grid Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="CONTINUE" IsEnabled="{Binding IsPauseContinueEnabled}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,4,0" Tag="continue" PreviewMouseDown="HoldButton_PreviewMouseDown" PreviewMouseUp="HoldButton_PreviewMouseUp" MouseLeave="HoldButton_MouseLeave"/>
                    <Button Grid.Column="1" Content="STOP" Command="{Binding StopRunCommand}" Style="{StaticResource DangerButtonStyle}" Margin="4,0,4,0"/>
                    <Button Grid.Column="2" Content="PAUSE" IsEnabled="{Binding IsPauseContinueEnabled}" Style="{StaticResource SecondaryButtonStyle}" Margin="4,0,0,0" Tag="pause" PreviewMouseDown="HoldButton_PreviewMouseDown" PreviewMouseUp="HoldButton_PreviewMouseUp" MouseLeave="HoldButton_MouseLeave"/>
                </Grid>
                <TextBlock Text="{Binding ConnectionMeta}" Foreground="{DynamicResource MutedBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel>
                    <TextBlock Text="System Events" Style="{StaticResource PanelTitleStyle}"/>
                    <TextBlock Text="DXF run progress, errors and connection events" Style="{StaticResource PanelSubtitleStyle}"/>
                </StackPanel>
                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Events}"
                         Margin="0,8,0,0"
                         Background="{DynamicResource PanelAltBrush}"
                         BorderBrush="{DynamicResource BorderBrush}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2">
                                <TextBlock>
                                    <Run Text="{Binding Time}"/>
                                    <Run Text="  "/>
                                    <Run Text="{Binding Title}" FontWeight="Bold"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Message}" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
