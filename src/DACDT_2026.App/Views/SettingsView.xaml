<UserControl x:Class="DACDT_2026.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1.12*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource PanelBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="DXF Processing" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="Motion and laser parameters for DXF paths" Style="{StaticResource PanelSubtitleStyle}"/>
                        <UniformGrid Columns="2" Margin="0,10,0,0">
                            <StackPanel Margin="0,0,6,6">
                                <TextBlock Text="Offset X (mm)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding OffsetXInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="6,0,0,6">
                                <TextBlock Text="Offset Y (mm)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding OffsetYInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,6,6">
                                <TextBlock Text="Travel Speed (M03 / Home) (mm/min)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding GlobalSpeedM3Input, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="6,0,0,6">
                                <TextBlock Text="Test Area Speed (mm/min)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding TestEngraveSpeedInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,6,6">
                                <TextBlock Text="Laser On Delay (M03) (ms)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding GlobalDwellM3Input, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="6,0,0,6">
                                <TextBlock Text="Laser Off Delay (M04) (ms)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding GlobalDwellM4Input, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,6,6">
                                <TextBlock Text="Engrave Speed (mm/min)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding EngraveSpeedInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="6,0,0,6">
                                <TextBlock Text="Engrave Power (%)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding EngravePowerInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,6,6">
                                <TextBlock Text="Cut Speed (mm/min)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding CutSpeedInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="6,0,0,6">
                                <TextBlock Text="Cut Power (%)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding CutPowerInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </UniformGrid>
                        <Button Content="Apply DXF Settings" Command="{Binding ApplyDxfSettingsCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource PanelBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="G-code Motion" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="Motion speeds applied to G-code paths" Style="{StaticResource PanelSubtitleStyle}"/>
                        <UniformGrid Columns="2" Margin="0,10,0,0">
                            <StackPanel Margin="0,0,6,6">
                                <TextBlock Text="Laser-On Speed (M03) (mm/min)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding GcodeSpeedM3Input, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="6,0,0,6">
                                <TextBlock Text="Rapid Travel Speed (G00) (mm/min)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding RapidSpeedInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </UniformGrid>
                        <Button Content="Apply G-code Settings" Command="{Binding ApplyGcodeSettingsCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource PanelBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Workspace" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="Drawing boundary used by the machine preview" Style="{StaticResource PanelSubtitleStyle}"/>
                        <UniformGrid Columns="2" Margin="0,10,0,0">
                            <StackPanel Margin="0,0,6,0">
                                <TextBlock Text="Workspace Width (mm)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding WorkspaceWidthInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Margin="6,0,0,0">
                                <TextBlock Text="Workspace Height (mm)" Foreground="{DynamicResource MutedBrush}"/>
                                <TextBox Text="{Binding WorkspaceHeightInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </UniformGrid>
                        <Button Content="Apply Workspace" Command="{Binding SetWorkspaceCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,8,0,0"/>
                        <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource PanelBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="G54-G59 WCS Offsets" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="Offsets added to G-code coordinates" Style="{StaticResource PanelSubtitleStyle}"/>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="180"/>
                            </Grid.ColumnDefinitions>

                            <DataGrid x:Name="WcsGrid"
                                      ItemsSource="{Binding WcsOffsets}"
                                      SelectedValue="{Binding ActiveWcs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      SelectedValuePath="Name"
                                      SelectionChanged="WcsGrid_SelectionChanged"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      SelectionMode="Single"
                                      SelectionUnit="FullRow"
                                      Height="240"
                                      Margin="0,0,10,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="WCS" Binding="{Binding Name}" IsReadOnly="True" Width="80"/>
                                    <DataGridTextColumn Header="Offset X (mm)" Binding="{Binding OffsetX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                    <DataGridTextColumn Header="Offset Y (mm)" Binding="{Binding OffsetY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Active WCS" Foreground="{DynamicResource MutedBrush}"/>
                                <Border Background="{DynamicResource PanelAltBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="12,9"
                                        Margin="0,4,0,8">
                                    <TextBlock Text="{Binding ActiveWcs}" FontSize="18" FontWeight="SemiBold"/>
                                </Border>
                                <Button Content="Apply WCS Table"
                                        Command="{Binding SetWcsCommand}"
                                        Style="{StaticResource PrimaryButtonStyle}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource PanelBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Configuration Profiles" Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="Save, load or delete settings profiles" Style="{StaticResource PanelSubtitleStyle}"/>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding ProfileNameInput, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" Margin="0,0,8,0"/>
                            <Button Grid.Column="2" Content="Save" Command="{Binding SaveProfileCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,6,0"/>
                            <Button Grid.Column="3" Content="Load" Command="{Binding LoadProfileCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0"/>
                            <Button Grid.Column="4" Content="Delete" Command="{Binding DeleteProfileCommand}" Style="{StaticResource DangerButtonStyle}"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
