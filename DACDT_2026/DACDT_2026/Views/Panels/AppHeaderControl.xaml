<UserControl x:Class="DACDT_2026.Views.Panels.AppHeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DACDT_2026">
    <Border Style="{StaticResource PanelBorderStyle}" Padding="12,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="34"
                        Height="34"
                        CornerRadius="8"
                        Background="{StaticResource AccentBrush}"
                        Margin="0,0,10,0">
                    <TextBlock Text="DC"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="White"/>
                </Border>
                <StackPanel>
                    <TextBlock Text="DACDT 2026" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="Gantry Robot Control System"
                               Foreground="{StaticResource MutedBrush}"
                               FontSize="12"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="10"
                         Height="10"
                         Fill="{Binding IsConnected, Converter={StaticResource BoolToStatusBrushConverter}}"
                         Margin="0,0,8,0"/>
                <TextBlock Text="{Binding ConnectionBanner}"
                           FontWeight="SemiBold"
                           Margin="0,0,18,0"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ActiveNotice}"
                           Foreground="{StaticResource MutedBrush}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="560"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
